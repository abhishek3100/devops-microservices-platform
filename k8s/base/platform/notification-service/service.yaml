apiVersion: v1
kind: Service
metadata:
  name: notification-service
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
      protocol: TCP
    - name: metrics
      port: 8000
      targetPort: 8000